{
  "default_strategy": "markdown_headings",
  "strategies": {
    "markdown_headings": {
      "description": "Split Markdown files at headings so each chunk keeps one idea together.",
      "max_tokens": 500,
      "overlap_tokens": 60
    },
    "code_by_symbol": {
      "description": "Split code by function or class so each chunk stays focused.",
      "max_tokens": 300,
      "overlap_tokens": 30
    },
    "fixed_fallback": {
      "description": "Use this only when the document has almost no structure.",
      "max_tokens": 400,
      "overlap_tokens": 40
    }
  },
  "metadata_fields": [
    "source_path",
    "doc_type",
    "topic",
    "effective_date"
  ]
}
